#!/usr/bin/env python
# coding: utf-8

import warnings
from typing import Any, Dict, List, Optional

import cobra
import numpy as np
import pandas as pd
from tqdm import tqdm
pd.options.mode.chained_assignment = None

with warnings.catch_warnings():
    warnings.simplefilter('ignore')
    from human_me.io import HiddenPrints
    from human_me.utils import functions as func

    from human_me.preprocess import parse_complex

    import human_me.expression.gene_expression.build_mrna_expression_reactions as build_mrna
    from human_me.expression.gene_expression import gene_information
    from human_me.expression.gene_expression.protein_expression import build_protein_expression_reactions as build_protein

def get_all_expression_reactions(model_metabolites, hgnc_id: str, reactions: List[cobra.Reaction], ub_args: Dict[str, Any], psim: pd.DataFrame, machinery_list: List[str],
                                modified_trna_transcript_c, charged_trna_map,
                                 compress_mrna: bool = False, nonmachinery_locations: Optional[List[str]] = None, stochastic: bool = False, seed: Optional[int] = None):
    """Generates all the expression reactions for a given protein from the HGNC ID and the PSIM.

    Parameters
    ----------
    me_input_model : cobra.Model
        the corrected input metabolic model (as provided in preprocess.correct_inputs.correct_model)
    model_metabolites : utils.metabolites.MetaboliteBin
        the me_input_model metabolites as specified by MetaboliteBin
    hgnc_id : str
        gene HGNC ID
    reactions : List[cobra.Reaction]
        list of cobra reactions
    ub_args : Dict[str, Any]
        set of variables generated by this ubiquitin.express_ubiquitin to be used throughout model building
    psim : pd.DataFrame
        see PSIM_README
    machinery_list : List[str]
        each entry is the HGNC ID of a protein that should be considered as catalyzing a reaction
    modified_transcript_c : macromolecules.RNA.tRNA
        output of create_trna()
    charged_trna_map : Dict[str, macromolecules.RNA.tRNA]
        output of creat_trna()
    compress_mrna : bool, optional
        whether to condense elongation, processing, and nuclear export reactions into a single reaction, by default False
    nonmachinery_locations : List[str], optional
        each entry is a one-letter code speficying subcellular location of the protein product for compartments it does not catalyze reactions in, by default None
    stochastic : bool, optional
         Whether a potentially stochastic output should be stochastic, or choose a default behavior instead, by default False
    seed : Optional[int], optional
        A seed for if stochastic is set to True, by default None

    Returns
    -------
    List[ExpressionReaction]
        the reactions required to synthesize the gene
    List[Protein]
        the protein macromolecules associated with the final locations that the gene should be expressed in 
    """
    if nonmachinery_locations is None:
        nonmachinery_locations = list()
    with warnings.catch_warnings():
        warnings.simplefilter('ignore')
        with HiddenPrints():
            gene_info = gene_information.generate_from_psim(hgnc_id=hgnc_id, psim=psim, machinery_list=machinery_list, reactions=reactions,
                                                            nonmachinery_locations=nonmachinery_locations,
                                                            stochastic=stochastic, seed=seed)
            mrna_reactions, mrna_transcript_c, mrna_deg_proxy = build_mrna.get_mrna_expression_reactions(gene_info=gene_info,
                                                                                                        model_metabolites=model_metabolites,
                                                                                                         compress_mrna=compress_mrna)
            protein_reactions, protein_metabolites = build_protein.get_protein_expression_reactions(gene_info,
                                                                                                    mrna_transcript_c,
                                                                                                    mrna_deg_proxy,
                                                                                                    ub_args=ub_args, 
                                                                                                    modified_trna_transcript_c=modified_trna_transcript_c, 
                                                                                                    charged_trna_map=charged_trna_map, 
                                                                                                    model_metabolites=model_metabolites)

    return mrna_reactions + protein_reactions, protein_metabolites


def emm_par(me_input_model, hgnc_id, gene_reaction_map, psim, machinery_list, ub_args, modified_trna_transcript_c, charged_trna_map, compress_mrna, non_machinery, stochastic, seed):
    # None bc will add later for expression model specific to this
    nml = list()
    if hgnc_id in non_machinery:
        nml = non_machinery[hgnc_id]
    expr_reactions, protein_metabolites = get_all_expression_reactions(me_input_model=me_indput_model, 
                                                                        hgnc_id=hgnc_id,
                                                                        psim=psim, 
                                                                        machinery_list=machinery_list,
                                                                        modified_trna_transcript_c=modified_trna_transcript_c, 
                                                                        charged_trna_map=charged_trna_map,
                                                                       reactions=gene_reaction_map[hgnc_id],
                                                                       compress_mrna=compress_mrna,
                                                                       ub_args=ub_args, nonmachinery_locations=nml,
                                                                       stochastic=stochastic, seed=seed)
    id_protein_map = {p.compartment: p for p in
                      protein_metabolites}  # store compartments and metabolite objects for each gene
    return id_protein_map, expr_reactions


def get_expression_machinery(reactions):
    gene_reaction_map = func.create_gene_reaction_map(reactions)
    expression_machinery_me = list(gene_reaction_map)
    if 'ribosome' in expression_machinery_me:
        expression_machinery_me.remove('ribosome')
    return gene_reaction_map, expression_machinery_me


def parse_complex_degradation_reaction_id(r_id: str) -> str:
    """Generates universal reaction ID analagous to func.parse_me_reaction_id specifically for ComplexDegradationReaction"""
    if r_id.count('_COMPLEX_') == 1:
        return r_id[r_id.index('_COMPLEX_') + len('_COMPLEX_'):]

    return '_'.join(r_id.split('_')[[i for i in range(len(r_id.split('_'))) if r_id.split('_')[i] == 'COMPLEX'][-1] + 1:])


def get_ko(mach_: List[str], knock_out: List[str]):
    """Determine whether a machinery list intersects with a knock_out genes list"""
    if len(knock_out) == 0 or len(set(mach_).intersection(knock_out)) == 0:
        return False
    return True


def get_complex_df(reactions: List[cobra.Reaction], knock_out: List[str]) -> pd.DataFrame:
    """Generate the complex df

    Parameters
    ----------
    reactions : List[cobra.Reaction]
        a list of reactions
    knock_out : List[str]
        each element is the HGNC ID of a gene expressed in the model which should be knocked out

    Returns
    -------
    complex_df : pd.DataFrame
        dataframe organizing complex information according to GPRs
    """
    complex_df = pd.DataFrame(
        columns=['reaction_id', 'compartment', 'machinery', 'is_complex', 'creates_multiple_reactions',
                 'knock_out'])

    for r in tqdm(reactions):
        compartment_ = r.enzyme_compartment

        ko = True
        if len(r.genes) == 1:
            ko = get_ko(mach_=[list(r.genes)[0].id], knock_out=knock_out)
            complex_df.loc[complex_df.shape[0], :] = [r.id, compartment_, list(r.genes)[0].id, False, False, ko]
        elif 'and' in r.gene_reaction_rule and 'or' in r.gene_reaction_rule:
            machinery_final = parse_complex.eval_complex(r.gene_reaction_rule)
            for m in machinery_final:
                if type(m) == list:
                    ko = get_ko(mach_=m, knock_out=knock_out)
                    complex_df.loc[complex_df.shape[0], :] = [r.id, compartment_, ';'.join(m), True, True, ko]
                else:
                    ko = get_ko(mach_=[m], knock_out=knock_out)
                    complex_df.loc[complex_df.shape[0], :] = [r.id, compartment_, m, False, True, ko]
        elif 'or' in r.gene_reaction_rule:
            machinery = [g.id for g in list(r.genes)]
            for m in machinery:
                ko = get_ko(mach_=[m], knock_out=knock_out)
                complex_df.loc[complex_df.shape[0], :] = [r.id, compartment_, m, False, True, ko]
        elif 'and' in r.gene_reaction_rule:
            m = sorted([g.id for g in r.genes])
            ko = get_ko(mach_=m, knock_out=knock_out)
            complex_df.loc[complex_df.shape[0], :] = [r.id, compartment_, ';'.join(m), True, False, ko]
        else:  # no genes
            pass

    return complex_df

# functions for minimize proteome method


def explode(df_, explode_cols, sep=None, fill_value=float('nan'), preserve_index=False):
    """Split entries with multiple values separated by a separator into multiple rows (or entries in lists)

    https://stackoverflow.com/questions/12680754/split-explode-pandas-dataframe-string-entry-to-separate-rows

    df_: pd.DataFrame
    explode_cols: list
        columns to split
    sep: separator
        if None, already in list format
    fill_value: 
        if empty entry, what to fill it with
    preserve_index: bool
        keep original index values for each new row

    """
    res = None
    for col in explode_cols:
        if res is None:
            df = df_.copy()
        else:
            df = res.copy()

        df[col] = df[col].str.split(sep)

        idx_cols = df.columns.difference([col])
        # calculate lengths of lists
        lens = df[col].apply(lambda x: len(x))

        idx = np.repeat(df.index.values, lens)
        # create "exploded" DF
        res = (pd.DataFrame({
            col_: np.repeat(df[col_].values, lens)
            for col_ in idx_cols},
            index=idx)
            .assign(**{col_: np.concatenate(df.loc[lens > 0, col_].values)
                       for col_ in [col]}))
        # append those rows that have empty lists
        if (lens == 0).any():
            # at least one list in cells is empty
            res = (res.append(df.loc[lens == 0, idx_cols], sort=False)
                   .fillna(fill_value))
        # revert the original index order
        res = res.sort_index()
        # reset index if requested
        if not preserve_index:
            res.reset_index(drop=True, inplace=True)
    return res


def map_machinery_compartment(df: pd.DataFrame) -> Dict[str, str]:
    map_ = df.groupby('machinery')['compartment'].apply(lambda x: list(set(x))).reset_index()
    map_ = dict(zip(map_.machinery, map_.compartment))
    return map_


def map_complex_machinery_compartment(df: pd.DataFrame) -> Dict[str, str]:
    df = df[df.is_complex & ~df.knock_out.astype(bool)][['compartment', 'machinery']]
    df = explode(df, explode_cols=['machinery'], sep=';')
    return map_machinery_compartment(df)


def merge_maps(map1, map2):
    merged_map = map1.copy()
    for k, v in map2.items():
        if k not in map1:
            merged_map[k] = v
        else:
            merged_map[k] = list(set(map1[k] + v))
    return merged_map
